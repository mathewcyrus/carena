import 'package:flutter/material.dart';

class CarSellerProfilePage extends StatelessWidget {
  const CarSellerProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("profile"),
      ),
    );
  }
}
